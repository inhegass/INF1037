#include <stdio.h>

int MediaAluno(float x, float y) {
	float media;
	media = (x * 3 + y * 7) / 10;
	return media;
}

int main(void) {
	float n1;
	float n2;
	printf("Qual a primeira nota?");
	scanf("%d", &n1);
	printf("Qual a segunda nota?");
	scanf("%d", &n2);
	MediaAluno(n1, n2);
}
